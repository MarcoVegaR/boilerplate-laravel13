<?php

use App\Models\{{ model }};
use App\Models\User;
use Database\Seeders\RolesAndPermissionsSeeder;
use Database\Seeders\{{ moduleStudly }}{{ resourceStudly }}PermissionsSeeder;

beforeEach(function () {
    $this->seed(RolesAndPermissionsSeeder::class);
    $this->seed({{ moduleStudly }}{{ resourceStudly }}PermissionsSeeder::class);
});

it('stores a {{ resourceHeadline }} record', function () {
    $user = User::factory()->withSuperAdmin()->withTwoFactor()->create();

    $this->actingAs($user)
        ->post(route('{{ routeNamePrefix }}.store'), [
{{ testsCreateAssertions }}
        ])
        ->assertRedirect();

    expect({{ model }}::query()->count())->toBe(1);
});
